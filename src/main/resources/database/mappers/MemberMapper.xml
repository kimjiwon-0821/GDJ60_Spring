<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.iu.s1.member.MemberDAO">
  	<insert id="setMemberAdd" parameterType="MemberDTO">
  		INSERT INTO BANKMEMBERS VALUES(#{id},#{pw},#{name},#{phone},#{email})
  	</insert>
  	<select id="getMemberList" resultType="MemberDTO" statementType="PREPARED">
  		SELECT *FROM BANKMEMBERS
  	</select>
  	<insert id="setMemberRoleAdd" parameterType="MemberDTO">
  		INSERT INTO MEMBER_ROLE(ID,ROLENUM) VALUES(#{id},(SELECT ROLENUM FROM ROLE WHERE ROLENAME='MEMBER'))
  	</insert>  
  	<select id="getMemberLogin" resultType="MemberDTO" parameterType="MemberDTO">
  		SELECT * FROM BANKMEMBERS WHERE ID=#{id}
  	</select>
  	<select id="getMemberPage" resultType="MemberDTO" parameterType="MemberDTO">
  		SELECT * FROM BANKMEMBERS WHERE ID=#{id}
  	</select>
  	<update id="setMemberUpdate" parameterType="MemberDTO">
  		UPDATE BANKMEMBERS SET NAME=#{name}, PHONE=#{phone},EMAIL=#{email} WHERE ID=#{id}
  	</update>
  	
  </mapper>